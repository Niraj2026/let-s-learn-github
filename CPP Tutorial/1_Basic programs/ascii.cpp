//to print the ASCII value of character.

#include <iostream>
using namespace std;

int main () {
    char c;
    cout <<"Enter character " ;
    cin >> c;
    cout << " The ASCII value of " << c << " is " << int(c);
    return 0;
}